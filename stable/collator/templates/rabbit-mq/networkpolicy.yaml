apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "collator.fullnameRabbitmq" . }}
  labels:
    {{- include "collator.labelsRabbitmq" . | nindent 4 }}
spec:
  ingress:
    - from:
      - podSelector:
          matchLabels:
            {{- include "collator.selectorLabelsApi" . | nindent 12 }}
      - podSelector:
          matchLabels:
            {{- include "collator.selectorLabelsEngine" . | nindent 12 }}
      ports:
        - protocol: TCP
          port: {{ .Values.rabbitmq.service.port }}
  podSelector:
    matchLabels:
      {{- include "collator.selectorLabelsRabbitmq" . | nindent 6 }}
  policyTypes:
    - Ingress
